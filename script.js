/* function primeiraFaseJake () {
    while (opcao != 1 || opcao != 2) {
        var opcao = prompt (
            `Escolha uma das opções:\n1. \n2. `);


        if (opcao == 1) {
            window.location.href = "../";
            break;

        } else if (opcao == 2) {
            window.location.href = "../";
            break;

        } else {
            alert (``);
        }
    }
} */